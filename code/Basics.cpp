# include <iostream>
# include <cmath>
using namespace std;

// defining a set of constants
// enum day{monday, tuesday, wendesday, thursday, friday, saturday, sunday};

// typedef int marks;

int main(){

    // area of a triangle
    // int b=7,h =5;
    // float area;

    // area = (float) b*h/2;

    // cout<<area;
    // return 0;

    // Sum of first N Natural Numbers
    // int n = 40,sum;

    // sum = (float) n*(n+1)/2;

    // cout<<sum;

    // Baskara Root
    // int a = 4,b = 2,c = 1,xp,xm;

    // xp = (-b + sqrt(b*b - 4*a*c))/(2*a);
    // xm = (-b - sqrt(b*b - 4*a*c))/(2*a);

    // cout<<"The roots are:"<<xm<<" "<<xp;

    // Area of a circle
    // float r,area,pi;

    // cout<<"Raio do circulo"<<endl;
    // cin>>r;

    // pi=3.1425f;
    // area = pi * r * r;

    // cout<<"Area is: "<<area<<endl;

    // Run Distance
    // float distance,v = 6,u = 8,a = 3;

    // distance = (float) (v*v - u*u)/(2*a);

    // cout<<distance<<endl;

    // Simple Interest Formula
    // float SI,T = 5,P = 6,R = 7;

    // SI = (float) (T*P*R)/100;

    // cout<<SI;

    // Volume of a Cilinder
    // float radius = 5,height = 8, pi=3.14f,volume;

    // volume = (float) (radius * radius) * pi *height;

    // cout<<volume;

    // Distance between 2 points
    // int x1=2,y1=8,x2=4,y2=6;
    // float dist;

    // dist = sqrt( (x2-x1)*(x2-x1) + (y2-y1) * (y2-y1) );

    // cout<<dist;

    // Enum example
    // day d;
    // d = monday;
    // cout<<friday<<endl;

    //Typedef example
    // marks m1,m2;
    // m1 = 60;
    // m2 = 75;

    // Net Salary Exercise
    // float salary, allowances, deductions;
    // cout<<"Input Basic salary";
    // cin>>salary;

    // cout<<"Input allowances (percentage)";
    // cin>> allowances;

    // cout<<"Input deductions (percentage)";
    // cin>>deductions;

    // salary = salary + ((salary * allowances)/100) - ((salary * deductions)/100);

    // cout<<"Final salary is:"<<salary<<endl;

    int a =25, b=9;
    cout<<(~a);
}
